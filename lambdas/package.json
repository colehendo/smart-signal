{
  "name": "lambdas",
  "version": "1.0.0",
  "description": "To import python packages:  - cd to lambdas/src/packages  - run: `pip install --target=./ <package-name>`",
  "main": "src/populate.py",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-python-requirements": "^5.0.1"
  }
}
